# 2026-07-10 · Session 4 of day — Retire "upcoming decisions" terminology (#30)

Closed **#30**: aligned the whole codebase with the user-facing "upcoming committee
action" language the UI already used, so terminology no longer drifts between the
copy and the code/docs.

## Done

- **File rename** (`git mv`, history preserved): `lib/decisions.ts` →
  `lib/committee-actions.ts`, `lib/decisions.test.ts` → `lib/committee-actions.test.ts`.
- **Identifiers**: `UpcomingDecision` → `UpcomingCommitteeAction`;
  `buildUpcomingDecisions`/`fetchUpcomingDecisions`(`Live`) → `…CommitteeActions`;
  `decisionRoleLabel` → `committeeActionRoleLabel`; `RepProfile.upcomingDecisions` →
  `upcomingCommitteeActions`; `Decisions` component → `CommitteeActions` (heading
  "Upcoming committee action"); log prefix `[decisions]` → `[committee-actions]`.
  Import paths + cross-file comment references updated (`events-index.ts`,
  `floor-schedule.ts`, `rep-profile.ts`).
- **UI copy**: `FloorThisWeek.tsx` "Unlike committee decisions…" → "…committee
  action".
- **Docs**: README one-liner, spec (§goal, §2.2 "Upcoming matters more than recent",
  §MVP scope, recess section), MAP.md (tree entry + both annotations).
- **Gate**: `npx tsc --noEmit` clean; `npm test` → 136 passing (18 files).

## Decisions (one line each — see decisions.md)

- Renamed the file `lib/decisions.ts` rather than only its exports — the shared name
  with the `decisions.md` rationale log was itself a source of confusion; the rename
  removes it.
- Left generic-English "decision" in the spec's thesis prose and all `decisions.md`
  references untouched — only the product noun-phrase was retired, per the issue.

## Issues

- Closed **#30** (via commit `fixes #30`).
- None opened — the rename was self-contained with no follow-ups.

## Transcript

`sessions/2026-07-10-4.transcript.md`
