# 2026-07-09 — Session 10-of-day: resolve #24 (utility gap vs. Congress.gov)

Strategy meeting, no code changed. Transcript: `sessions/2026-07-09-10.transcript.md`.

## Done

- **Resolved #24** (why not just use Congress.gov directly). Pressure-tested the spec's
  four claimed differentiators against the *rendered* page (`AddressLookup` →
  `buildProfilesAction` → `RepSection`) — all four delivered, but none named to a
  first-time visitor. Reframed the moat as the **selection + ordering layer**
  (routing/timing), not content — we render CRS text verbatim and link back, so we
  can't out-content the source. Recorded the one-sentence answer of record and the
  rationale in `decisions.md`.
- Two owner decisions: **UI framing kept implicit** (no landing/marketing block — would
  read salesy and step on the "not affiliated" disclaimer); the **empty
  upcoming-decisions state** is the one real "worse Congress.gov" risk → filed as **#27**.
- User raised a committee-pipeline feature ("bills stuck/parked in a rep's committee").
  Strong thesis fit but already existed as **#21**. Folded this session's additions into
  #21 (no editorial "stuck" label — structural status facts only; reuse §2.3
  procedural-activity filter as the signal cut; doubles as the #27 empty-state filler)
  and closed the duplicate I'd filed (#28) as a dup of #21.

## Decisions (rationale → decisions.md)

- #24 utility gap: moat is routing/timing not content; UI framing kept implicit; empty
  state is the real risk → #27. (2026-07-09 entry in `decisions.md`.)

## Issues

- Closed: **#24** (resolved as recorded editorial position + #27).
- Opened: **#27** (empty/recess state pivots to contact + recent activity, not a bill dump).
- Opened then closed as duplicate of #21: **#28** (committee pipeline) — additions folded
  into **#21**.

## Next

Remaining strategy Issues #25 (design) / #26 (compliance); build side #23 → #4/#8.
#27 and #21 are strong post-MVP adds surfaced here.
