# 2026-07-10 · Session 1 (of day) — pitch accuracy reword

## What was done

- Questioned the "upcoming decisions" pitch. Traced it to `lib/decisions.ts`: an
  "upcoming decision" is a committee meeting the rep has a role in — markups (real
  decisions) but also hearings (testimony, no vote). So "decisions" overclaims.
- First reword to "upcoming committee action". Owner then noted a rep card also
  renders sponsored bills (`RepSection.tsx`), so "committee action" *underclaims*.
- Landed on a broader, accurate pitch: *"see what your federal representatives are
  working on — the committee action ahead and the bills they're sponsoring — in
  time to act."* Applied to `app/page.tsx` and `app/layout.tsx`; `AddressLookup.tsx`
  loading string softened to "upcoming action". Build + typecheck green.
- Scoped the change to user-facing copy only; deferred the deeper terminology
  cleanup and a privacy-copy audit to Issues (see below).

## Decisions made (rationale → decisions.md)

- Reword the homepage pitch away from "decisions" for accuracy, and broaden it to
  cover sponsored bills, not just committee action.
- Ship copy-only now; track the identifier/spec/README rename as one Issue rather
  than a piecemeal edit, to avoid half-renamed terminology drift.

## Issues

- Opened **#30** — retire "upcoming decisions" in code identifiers, spec, README
  (adopt the same broadened pitch, not just a noun swap).
- Opened **#31** — reread/improve the privacy disclaimer (third-party disclosure,
  "never logged" claim, no address-derived storage, Footer↔spec alignment).

## Transcript

`sessions/2026-07-10-1.transcript.md`
