# 2026-07-12 — Weekly reconciliation

Second reconciliation (first was Session 11, `2026-07-09-5.md`). Audited STATUS.md,
spec.md, and MAP.md against the code and `git log` by running the Derived-Facts commands
plus ad-hoc greps.

## Checked (all by command, not recall)

- `npm test` → **136 passing, 18 files** (Vitest 4.1.10) — matches STATUS.
- `npx tsc --noEmit` → exit 0 — matches STATUS.
- `find app -name route.ts -o -name page.tsx` → `app/api/cron/prewarm/route.ts`,
  `app/api/health/route.ts`, `app/page.tsx` — matches STATUS.
- `gh issue list` → open: **#18, #21, #25, #26, #29**; MVP milestone **1 open / 20 closed**
  — matches STATUS. No closed issue is described as open in any doc, and no open issue as
  closed.
- `git ls-files` → full tree vs MAP tree.

## Found

1. **MAP.md tree was badly stale** (last regenerated 2026-07-09). Missing ~10 real paths
   added since: `lib/session-status.ts`, `floor-schedule.ts`, `events-index.ts`,
   `district-offices.ts`, `rate-limit.ts`, `prewarm.ts`; `app/FloorThisWeek.tsx` (+test),
   `app/ExternalLink.tsx`, `app/RepSection.test.tsx`, `app/api/cron/prewarm/route.ts`;
   top-level `vercel.json` and `.github/workflows/prewarm.yml`. Counts also drifted:
   fixtures 21→**26**, journal 10→**30**, co-located lib test files 9→**15**.
2. **STATUS.md Git derived-fact row** lagged HEAD by one commit (`09c7991` vs actual
   `8c54476`).
3. **decisions.md** — no buried decisions. Every "chose X over Y" in journals since
   Session 11 was already folded by /end-session (through #9, 2026-07-10). §3 no-op.
4. **spec.md** — no false claims. (Minor: Privacy §still names "Anthropic receive no
   address" though the LLM was retired session 6 — factually true, left as-is, not drift.)

## Fixed (all in place)

- Regenerated MAP.md tree wholesale from `git ls-files`; reconciled the annotation table:
  added rows for all new lib/app paths, `vercel.json`, and the prewarm workflow; updated
  `actions.ts` (added `buildProfilesAction`) and `AddressLookup.tsx` (renders
  `FloorThisWeek` too) rows; refreshed collapsed counts and the stamp to 2026-07-12.
- Corrected STATUS.md Git row to `8c54476`.

## Filed

Nothing. No non-trivial contradictions surfaced.

## Counts

Claims checked: ~10. Contradictions found: 2 (stale MAP tree, stale STATUS git row).
Fixed in place: 2. Filed as issues: 0.
