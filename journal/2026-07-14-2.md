# 2026-07-14 · Session 2 — Favicon: Capitol-dome mark (closed #18)

Picked #18 off the open list and shipped it. Replaced the placeholder favicon with a
considered Capitol-dome mark, closing the last open MVP-milestone Issue.

## What was done

- Reviewed the existing placeholder (`app/icon.svg` — slate rounded square, light-slate
  shield + checkmark from #10) and confirmed App Router auto-serves `app/icon.svg` with no
  `layout.tsx` icon override.
- Designed a **Capitol-dome** mark: white dome + finial, entablature beam, four columns,
  and a stepped base (`#f8fafc`) on the slate ground (`#0f172a`).
- First pass used five thin columns that merged to grey at 16×16; **redesigned bolder** —
  four chunky columns with real gaps, a larger dome, building filling more of the frame.
- Verified legibility by rendering at 16/32/256 and inspecting **nearest-neighbor**
  upscales (real tab pixels, not smoothed) — installed Pillow into a scratch env for the
  NN upscale since no ImageMagick/rsvg present.
- Committed + pushed `3a1661f`; `closes #18` auto-closed the Issue on push to `main`.

## Decisions (one line each)

- **Capitol dome over shield/pin/envelope/monogram** — clearest "federal representatives"
  signal and holds up as a solid silhouette at tab size (full rationale → `decisions.md`).
- **White dome on slate ground** (owner call) — dome-colored on a contrasting field reads
  better than the monochrome shield; stays consistent with the slate UI palette.
- **SVG-only for MVP; defer `apple-icon.png`** — one vector covers the 16/32 tab targets;
  raster siblings only matter for iOS home-screen / PWA, which MVP doesn't need. Not filed
  (explicitly "later if desired" in #18).

## Issues opened

None.

## Issues closed

- **#18** — placeholder favicon replaced with the Capitol-dome mark. Last open MVP-milestone
  Issue; the MVP milestone is now empty.

## Transcript

See `sessions/2026-07-14-2.transcript.md`.
