# 2026-07-14 (session 3) — Design pass (#25)

Third session of the day. Took the standing design-polish item (#25) from
"unstyled-Tailwind-default territory" to a considered aesthetic.

## Done

- **Locked the direction before polishing** (the issue's explicit ask). Put two
  owner decisions up front via a quick question round: aesthetic = **"modern
  clean"** (all-sans, white/slate ground, single indigo accent) over a
  serif-civic or warm-approachable direction; party affiliation = **plain text,
  no color-coding** (it already was text, so the neutral choice was low-risk).
- **Established primitives** (`globals.css`, `layout.tsx`): Inter/system sans,
  slate-50 ground with white cards, one consistent indigo accent, and a single
  app-wide `:focus-visible` ring replacing the ad-hoc per-component focus rings.
- **Masthead** (`page.tsx` + new `app/BrandMark.tsx`): reused the shipped
  Capitol-dome favicon (#18) as an inline wordmark logo.
- **Focal point** (`RepSection.tsx`): "Upcoming committee action" is now an
  indigo-tinted panel with an accent-dot heading and left-bordered hearing cards,
  reading above the quieter committees/contact/bills; intentional empty states
  for both in-session-nothing-scheduled and recess.
- **States** (`AddressLookup.tsx`): indigo CTA with inline spinner, spinner'd
  loading line, iconed error, hover-lit disambiguation cards with a chevron;
  contact rows and the form stack on mobile; unified card treatment with
  `FloorThisWeek.tsx`.
- **Verified** end-to-end: `tsc` clean, 136/136 tests pass, build succeeds;
  driven via Playwright on desktop + 375px mobile with a live DC lookup (Chrome
  extension wasn't connected, so used Playwright).

## Issues

- Closed **#25** (design pass) — shipped in `d3bacb8`.
- Opened **#38** — re-run the a11y audit against the new visual design
  (contrast/focus/axe/Lighthouse). Raised because the restyle touched every
  surface after #9's audit; owner approved filing it (the automated write gate
  had paused it first).

## Decisions

Two logged to `decisions.md`: the "modern clean" aesthetic, and party-as-plain-
text (no color-coding).

## Transcript

See `sessions/2026-07-14-3.transcript.md`.
