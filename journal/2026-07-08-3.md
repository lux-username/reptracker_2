# 2026-07-08 — Session 3

Short review-and-record session; no code changes.

## What was done

- Followed up on the TypeScript-7-vs-Next-16 build break flagged at the end of session 2. Confirmed it's already fully resolved: `typescript@5.9.3` pinned in `package.json`, `tsc` reports 5.9.3, and the decision is recorded in `decisions.md` (line 25). Nothing outstanding.
- Discussed whether to add a prebuild version guard so a future contributor can't reintroduce TS 7. **Decided against it** — the `^5.9.3` caret range already caps below the 7.x major, so an accidental bump can't happen; only an explicit `typescript@7` would, and that's a deliberate act with a clear trail in decisions.md.
- Discussed where "gotcha / constraint" notes belong. Reaffirmed the doc taxonomy (MAP.md is authority): such notes go to `decisions.md` (or `spec.md` for product constraints), surfaced transiently via `STATUS.md`, and **never** to CLAUDE.md (process + command-derived facts only). No repo change — the taxonomy already covers it.
- Saved a private cross-session memory capturing the "route notes via MAP.md's taxonomy; not CLAUDE.md" convention (in Claude's memory store, not the repo, per the user's preference).

## Decisions made

None new for `decisions.md` (the TS pin was recorded in session 2; the doc-routing convention lives in Claude's private memory, not the repo).

## Issues opened / closed

None. Open Issues unchanged: #2–#11 (MVP milestone).

## Next

Issue #2 — Address → reps lookup with disambiguation (Geocodio → district), the first feature and first external API.

Transcript: `sessions/2026-07-08-3.transcript.md`.
