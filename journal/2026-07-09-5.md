# 2026-07-09 — Session 11 (weekly reconciliation)

First drift audit run. Docs cross-checked against code + git log; findings fixed in
place (no code changed).

## What was checked

- **Derived Facts** (CLAUDE.md commands): `npm test` → 68 passing / 10 files;
  `npx tsc --noEmit` → exit 0; routes → `app/api/health/route.ts`, `app/page.tsx`;
  deploy live. All match STATUS.md.
- **spec.md** against the shipped code (LLM retirement, session 6).
- **MAP.md** tree against `git ls-files`.
- **decisions.md** for buried session decisions not yet folded.
- **STATUS.md** open-issue lists against `gh issue list`.

## Findings + disposition

- **spec.md was substantially stale (Issue #19) — FIXED in place.** The design-of-record
  still documented the retired LLM: the summary pipeline, the hallucination validator,
  the per-rep TL;DR, the LLM cost model + guardrails, the per-rep digest content hash,
  the Anthropic key/SDK, and assorted LLM mentions in the data-source table, editorial
  stance, known risks, and DoD. Code confirms the retirement — no `@anthropic-ai` in
  `package.json` or anywhere in the tree; `lib/summaries.ts` renders CRS summaries
  verbatim ("NO LLM"). Rewrote each stale section to the shipped CRS-verbatim design,
  pointing at the 2026-07-08 "Retired the LLM entirely" decision rather than
  re-explaining. Closed **#19**.
- **MAP.md tree predated the entire app — FIXED.** The derived tree still showed only
  the doc scaffold (init-workflow state); no `app/`, `lib/`, config, or `package.json`.
  Regenerated the tree from `git ls-files` and added curated annotation rows for every
  significant source path. Stamp updated.
- **STATUS.md — accurate, minor prune.** All derived facts held; only edit was dropping
  now-closed **#19** from its open-issue lists.
- **decisions.md — caught up.** Rationale through session 9 already folded; session 10
  had none. Nothing to add.

## Issues

- Closed **#19** (spec ↔ LLM-retirement reconciliation) — done here.
- None opened; every finding was trivially fixable in place.

## Counts

Claims checked across STATUS/spec/MAP/decisions: ~15 factual areas. Contradictions
found: 2 structural (spec LLM drift, MAP stale tree) + 1 minor (STATUS #19 listing).
All 3 fixed in place; 0 filed.

## Transcript

See `sessions/2026-07-09-5.transcript.md`.
