# 2026-07-14 · Session 1 — Feedback triage → backlog Issues

Triage-only session, no code changed. Relayed a batch of friend feedback (6 items) from
people who saw the live RepTracker site; decided per-item whether to fix now or file, and
filed.

## What was done

- Read the relevant code for each item to ground the triage: `app/page.tsx`,
  `app/FloorThisWeek.tsx`, `app/AddressLookup.tsx`, `app/RepSection.tsx`,
  `lib/floor-schedule.ts`, `lib/summaries.ts`, `lib/legislation.ts`.
- Triaged 6 feedback items into **5 Issues**. Nothing solved in place — each item was a
  spec reversal, sourced-content task, new feature with cost/privacy weight, or a
  design-placement call; none was a trivial unambiguous fix.

## Decisions (one line each)

- **File all six, fix none** — the product is MVP-complete and every item needed an owner
  call or research; consistent with "open work is an Issue."
- **Merged feedback items 2 + 6 into one Issue (#34)** — both are "make the House floor
  categories legible," same code (`FloorCategory.heading`), same source (docs.house.gov XML).
- (No architecture/product rationale to fold — nothing appended to `decisions.md`.)

## Issues opened

- **#33** — UX: hide "On the floor this week" until after lookup (reverses spec §2.3 / #4).
- **#34** — explain House floor categories + "may be considered" vs "scheduled vote" (items 2+6).
- **#35** — address autocomplete/typeahead (new provider; per-keystroke cost + privacy).
- **#36** — show bill policy-area / subject tags. Answered feedback item 4: Congress *does*
  tag bills (`policyArea` + `subjects`); `policyArea` already ingested in `lib/legislation.ts`.
  Flagged as the lightest lift / quick win for next session.
- **#37** — CRS summaries on floor-this-week bills (pipeline exists; N×2 API-call cost).

## Issues closed

None.

## Transcript

See `sessions/2026-07-14-1.transcript.md`.
